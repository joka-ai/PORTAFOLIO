data:
  input_path: "data/WA_Fn-UseC_-HR-Employee-Attrition.csv"
  work_dir: "data"

outputs:
  dir: "outputs"
  models_dir: "outputs/models"
  logs_dir: "logs"

modeling:
  test_size: 0.25
  random_state: 42
  optuna: true
  n_trials: 20
  smote: true
  seed: 42
  model_prefix: "attrition_model"
